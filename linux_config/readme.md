`.xinitrc` -> `~`
`user-dirs.dirs` -> `~/.config`
`dwm.desktop` -> `/usr/share/xsessions/` or `~/.local/share/xsessions/` // you may have to change the username in `Exec=` (see the file) // you will also have to make `~/.xinitrc` executable
`gpg-agent.conf` -> `~/.gnupg/gpg-agent.conf`
`.profile` -> `~`
